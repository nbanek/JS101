/*
A function that returns the sum of two numbers;
  Assign a value to two variables each.
  Pass those variables into a function
  Add the values
  Return the sum

START

SET number1 = some number
SET number2 = some number

READ number1, number 2

PRINT number 1 + number 2

END
*/

/*
A Function that takes an array of strings and concatenates them
  Pass an array of strings to a function
  Create an empty variable to be a string
  Iterate through the array
  Use the '+' operator to concatenate each iteration to the empty string
  return the resulting string

START
SET string to empty
READ array
WHILE some variable < array.length 
SET string += array[variable] 
PRINT string
END
*/